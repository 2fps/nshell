import React from 'react';
import { Input, message } from 'antd';

class Http extends React.Component {
    render() {
        return (
            <div>
            </div>
        );
    }
}

export default Http;