import React from 'react';
import {message, Button } from 'antd';

class Ping extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            result: {}
        };
    }
    render() {
        return (
            <div>没实现呢</div>
        );
    }
}

export default Ping;